# Dev tooling only — nothing here is deployed
node_modules/
test-results/
playwright-report/
blob-report/
.playwright/

# A second, separate clone of this repo that used to live inside the working
# copy, carrying its own .git and 64MB of duplicated mp3s. It was deleted on
# 2026-09-22 (see CLAUDE.md); this entry stays so it cannot come back by
# accident. The note here used to read "do NOT delete it", which is the
# opposite of what happened and of what should happen - one machine, one copy.
/bylx/

# Where the deploy workflow parks index.html so it can be uploaded last.
.entry/

# OS noise
Thumbs.db
desktop.ini
.DS_Store
